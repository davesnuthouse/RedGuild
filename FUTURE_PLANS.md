Ideas for future releases (significant work):

* Colour coding in the logs to make them easier to read (and full redesign of log tab tbh)
* Raid group planner
* Tactics
* are you back check?
* Tie handling. Two bidders at the same top amount — offer a one-click "roll off between these two" that prompts just them, rather than the host resolving it in chat.
* Auction queue. A boss drops 4-5 items and right now that's 4-5 manual posts. Let the auctioneer queue items (drag from the ML window or paste links), then run them back to back with a "3 of 7" indicator. Host saves a lot of clicking; bidders can see what's still coming and budget their DKP instead of blowing it on the first token.
* a button on the editor tab that reports back every online users addon version, and dkp table version
